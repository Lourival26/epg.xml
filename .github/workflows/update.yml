name: Atualizar EPG Lourival26

on:
  schedule:
    - cron: '0 3 * * *' # Este comando faz o robô rodar todo dia às 03:00 da manhã
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Instalar dependências
        run: pip install requests

      - name: Rodar o script Lourival26
        run: python main.py

      - name: Enviar alterações para o GitHub
        run: |
          git config --global user.name "Lourival26-Bot"
          git config --global user.email "bot@lourival26.com"
          # Adiciona os arquivos com ponto gerados pelo script
          git add epg.br.xml epg.pluto.xml
          # Só faz o commit se houver alterações em qualquer um dos arquivos
          git diff --quiet && git diff --staged --quiet || git commit -m "Automação: EPGs atualizados pelo robô Lourival26"
          git push
